# Workflow

Canonical content lives in `docs/`. Pointers only here.

| Topic | See |
|---|---|
| Agent roles, delegation, loop boundaries, where artifacts live | `docs/agents/index.md` |
| Per-slice workflow (branch → baseline → profile → spike → design → approve → implement → gate → record → merge) | `docs/refactoring-plan.md § Workflow per slice` |
| Correctness gate | `testing.md § Correctness regression gate` |
| Performance gate, timed-output validation, RSS acceptance | `testing.md`, `docs/refactoring-plan.md § Performance gate` |
| Non-goals for routine slices | `docs/refactoring-plan.md § Non-goals` |
