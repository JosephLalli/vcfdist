# Plan: Refactor vcfdist Monolithic Codebase

The per-slice workflow steps live in `docs/refactoring-plan.md § Workflow per slice`. This file tracks track-level status only.

## Status

- Track open; several optimization slices merged to `master` (see `docs/benchmark-progress.json` `slice_results`). No slice currently in progress.

Completed slices and tried-and-retired directions are tracked in `docs/refactoring-plan.md § Tried and retired` and `docs/benchmark-progress.json`.
